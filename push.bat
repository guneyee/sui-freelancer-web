@echo off
cd /d C:\Users\güney\sui-freelancer-web
git config user.email "guneykaraoz@hotmail.com"
git config user.name "guneyee"
git commit -m "Initial commit: Sui freelancer platform with GitHub IPA tier system"
git push -u origin main
pause
